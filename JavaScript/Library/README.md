# Odin - Library

Minimal library app built for **The Odin Project** (JavaScript course).

A tiny single-page app that lets you add books, mark them as read/unread, and delete them using a simple modal form.
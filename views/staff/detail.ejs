<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <p><%= staff.fullname %></p>
  <% if (staff.birth) { %>
    <p><%= format(new Date(staff.birth), 'PPP') %></p>
  <% } %>
  <% if (staff.gender) { %>
    <p><%= staff.gender %></p>
  <% } %>
  <p><b>Staff Roles</b></p>
  <ul>
    <% staff.manga.forEach(manga => { %>
      <li>
        <p><%= manga.title %> - <%= manga.role %></p>
        <a href="/<%= manga.id %>/detail">Details</a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% staff.forEach(function(member) { %>
      <li>
        <strong><%= member.fullname %></strong>
        <a href="/staff/<%= member.id %>">View Details</a>
      </li>
    <% }); %>
  </ul>
  <a href="/">Back to Home</a>
</body>
</html>